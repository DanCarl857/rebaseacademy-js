// Interaction: alert, prompt, confirm
//
// alert - showes a message and waits for the user to press "OK"
//
// prompt -> accepts 2 arguments
// title -> text to show the visitor
// and optional 2nd parameter -> initial value of the input field
//
result = prompt("What is your name?", "");
alert(`Welcome to our game ${result}`);
// confirm(`Are your sure your name is ${result}?`);

let isName = confirm("Are your sure?");

// Assignment:
// Create a web page that asks for a name and outputs it.
// Think of some other simple program you can write and do that.
