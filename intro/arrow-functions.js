// ES6
//
const sum = (a, b) => a + b;

// function sum (a, b) {
//  return a + b
// }
//
const showMessage = () => console.log("Hello");
const showMessage1 = () => {
  let message = "Hello ";
  console.log(message);
};

let double = (n) => n * 2;

// Assigment: rewrite all the functions in the previous assignment as arrow functions
